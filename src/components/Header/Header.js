import React from 'react';
import './Header.css';

const Header = props => {

    return (
        <header>
            <div className="logo"></div>
            <h1>Monitor de saúde</h1>
        </header>
    )
};

export default Header;